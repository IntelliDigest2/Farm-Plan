import React from "react";
import { connect } from "react-redux";

export const TurnOverChart = (props) => {
	return <div>TurnOverChart</div>;
};

const mapStateToProps = (state) => ({});

const mapDispatchToProps = {};

export default connect(mapStateToProps, mapDispatchToProps)(TurnOverChart);
